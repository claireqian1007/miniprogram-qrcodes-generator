# miniprogram-qrcodes-generator
auto generate qrcodes based on given path list in excel table

1. create a xlsx file which includes all mini program page path in one column
2. replace the correct appid, secret key, and the right path region in js file
3. define the qrcodes' name
4. run the js file, you will see a new folder including all the qrcodes based on given path